package com.example.onboarding.domain.model;

public enum CompanyStatus {
    INCOMPLETE,
    PENDING_ACTIVATION,
    ACTIVATION_PROCESSING,
    ACTION_REQUIRED,
    ACTIVE,
    ACTIVATION_FAILED,
    ACTIVATION_CANCELED,
    REQUIRES_SUPPORT
}
